using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// this script will hold a list of all the items in the players stash 
// this script can add and remove items but cannot edit any of the item inffo

public class playerStashInventory : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
