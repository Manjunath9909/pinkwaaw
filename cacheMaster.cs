using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//this is the script that handles the link between the saves files and the game
//will also cache data for ease of access

public class cacheMaster : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
